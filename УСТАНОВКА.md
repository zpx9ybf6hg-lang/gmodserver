# 🎮 GMod DarkRP Server - Простая установка на Oracle Cloud

**Время установки: 15 минут**

---

## Шаг 1: Создай VM на Oracle Cloud (5 минут)

1. Зайди на [cloud.oracle.com](https://cloud.oracle.com)
2. **Compute** → **Instances** → **Create Instance**

**Настройки:**
- **Name**: `gmod`
- **Image**: `Ubuntu 22.04`
- **Shape**: `VM.Standard.E2.1.Micro` ⚠️ **Always Free**
- **SSH Keys**: Скачай приватный ключ (понадобится!)
- **Networking**: Убедись что "Assign public IP" включено

3. Нажми **Create**
4. Дождись пока статус станет **Running** (1-2 минуты)
5. **Скопируй Public IP** (например: `152.70.18.104`)

---

## Шаг 2: Открой порты в Oracle (2 минуты)

1. **Networking** → **Virtual Cloud Networks**
2. Выбери твою VCN
3. **Security Lists** → **Default Security List**
4. **Add Ingress Rules** → Добавь 2 правила:

**Правило 1:**
```
Source CIDR: 0.0.0.0/0
IP Protocol: UDP
Destination Port: 27015
```

**Правило 2:**
```
Source CIDR: 0.0.0.0/0
IP Protocol: TCP
Destination Port: 27015
```

5. Нажми **Add Ingress Rules**

---

## Шаг 3: Подключись к VM (1 минута)

На твоём Mac:

```bash
# Замени путь к ключу и IP на свои!
ssh -i ~/Downloads/ssh-key-2025-12-28.key ubuntu@152.70.18.104
```

Введи `yes` когда спросит.

---

## Шаг 4: Установи сервер ОДНОЙ командой! (10 минут)

Просто **скопируй всё ниже** и вставь в терминал на Oracle VM:

```bash
curl -fsSL https://raw.githubusercontent.com/zpx9ybf6hg-lang/gmodserver/main/quick-install.sh | bash
```

Скрипт автоматически:
- ✅ Установит зависимости
- ✅ Установит SteamCMD
- ✅ Скачает GMod сервер (~6GB)
- ✅ Установит DarkRP
- ✅ Настроит всё

**Дождись сообщения: "✅ Установка завершена!"** (~10 минут)

---

## Шаг 5: Запусти сервер! (1 минута)

```bash
screen -S gmod
cd ~/gmodserver
./srcds_run -game garrysmod +maxplayers 16 +map rp_downtown_v4c_v2 +gamemode darkrp
```

**Готово!** Сервер запущен!

Чтобы выйти из screen (сервер продолжит работать):
- Нажми `Ctrl+A`, потом `D`

Чтобы вернуться к логам:
```bash
screen -r gmod
```

---

## 🎮 Подключись к серверу!

В Garry's Mod:
1. Открой консоль (`` ` ``)
2. Введи:
```
connect 152.70.18.104:27015
```

**Замени IP на свой!**

---

## ⚙️ Полезные команды

### Остановить сервер:
```bash
screen -r gmod
# Нажми Ctrl+C
```

### Запустить снова:
```bash
screen -S gmod
cd ~/gmodserver
./srcds_run -game garrysmod +maxplayers 16 +map rp_downtown_v4c_v2 +gamemode darkrp
```

### Изменить настройки:
```bash
nano ~/gmodserver/garrysmod/cfg/server.cfg
```

### Перезагрузить сервер:
```bash
# Остановить (Ctrl+C в screen)
# Запустить снова
```

---

## 🤖 Автоматическое обновление через GitHub (опционально)

Если хочешь чтобы сервер обновлялся при git push:

1. Создай SSH ключ:
```bash
ssh-keygen -t ed25519 -f ~/.ssh/github_actions -N ""
cat ~/.ssh/github_actions.pub >> ~/.ssh/authorized_keys
cat ~/.ssh/github_actions
```

2. Скопируй приватный ключ (от BEGIN до END)

3. Добавь секреты в GitHub:
- Зайди: https://github.com/zpx9ybf6hg-lang/gmodserver/settings/secrets/actions
- Создай секреты:
  - `ORACLE_HOST` = Твой Oracle IP
  - `ORACLE_USERNAME` = `ubuntu`
  - `ORACLE_SSH_KEY` = Приватный ключ

4. Теперь при `git push` сервер обновится автоматически!

---

## 🎉 Готово!

**Адрес твоего сервера:** `твой_IP:27015`

Приятной игры! 🎮
